{% liquid
  assign id = section.id
%}

<style>
  #shopify-section-{{id}} {
    box-sizing: border-box;
    margin-top: {{section.settings.margin_top | append: 'px'}};
    margin-bottom: {{section.settings.margin_bottom | append: 'px'}};
    padding-top: {{section.settings.padding_top | append: 'px'}};
    padding-bottom: {{section.settings.padding_bottom | append: 'px'}};
  }

  @media(max-width: 600px) {
    #shopify-section-{{id}} {
      margin-top: {{section.settings.margin_top_m | append: 'px'}};
      margin-bottom: {{section.settings.margin_bottom_m | append: 'px'}};
      padding-top: {{section.settings.padding_top_m | append: 'px'}};
      padding-bottom: {{section.settings.padding_bottom_m | append: 'px'}};
    }
  }
  #shopify-section-{{id}} {
    .wrapper-{{ id }}{
      display: flex;
      max-width: 100%;
      flex-direction: column;
      {% if section.settings.max_width != blank %}
      max-width: {{ section.settings.max_width }}px;
      {% else %}
      max-width: 100%;
      {% endif %}    
      margin: auto;  
      gap: var(--Spacing-Container-sm, 16px);
      {% if section.settings.content_alignment == 'left' %}
        align-items: start;
      {% elsif section.settings.content_alignment == 'right' %}
        align-items: end;
      {% else %}
        align-items: center;
      {% endif %}
    }
    h2.title {
      margin: 0 0 8px;
      font-family: var(--font-heading-family);
      font-weight: 700;
    }    
    .text-wrapper{
      width: 100%;   
      display: flex;
      {% if section.settings.content_alignment == 'left' %}
        justify-content: start;
      {% elsif section.settings.content_alignment == 'right' %}
        justify-content: end;
      {% else %}
        justify-content: center;
      {% endif %}
    }
    .text{
      {% if section.settings.content_alignment == 'left' %}
        text-align: left;
      {% elsif section.settings.content_alignment == 'right' %}
        text-align: right;
      {% else %}
        text-align: center;
      {% endif %}
      color: var(--Content-Base-main, #1F2937);
      margin: 0;
      /* font-family: var(--font-family-Body, "Neue Plak"); */
      font-size: var(--Font-Size-Paragraph-Medium, 18px);
      font-style: normal;
      font-weight: 400;
      line-height: var(--Line-Height-Paragraph-Medium, 26px); /* 144.444% */
    }
    .richtext-wrapper * {
      margin: 0 0 24px;
      @media(max-width: 768px) {
        margin: 0 0 16px;        
      }
    }
    .richtext-wrapper{
      {% if section.settings.content_alignment == 'left' %}
        text-align: left;
      {% elsif section.settings.content_alignment == 'right' %}
        text-align: right;
      {% else %}
        text-align: center;
      {% endif %}
      p{
        color: var(--Content-Base-main, #1F2937);
        /* font-family: var(--font-family-Control, "Neue Plak"); */
        font-size: var(--Font-Size-Paragraph-Regular, 14px);
        font-style: normal;
        font-weight: 400;
        line-height: var(--Line-Height-Paragraph-Medium, 30px); /* 150% */  
        margin: 0 0 var(--Spacing-Container-xs, 12px);
        strong{
          font-size: 18px;
        }
      }
      h3{
        {% if section.settings.content_alignment == 'left' %}
          text-align: left;
        {% elsif section.settings.content_alignment == 'right' %}
          text-align: right;
        {% else %}
          text-align: center;
        {% endif %}
        color: var(--Content-Base-main, #1F2937);
        {% comment %} margin: 0; {% endcomment %}
        /* font-family: var(--font-family-Title, "Neue Plak"); */
        font-size: var(--Font-Size-Heading-H3, 24px);
        font-style: normal;
        font-weight: 400;
        line-height: var(--Line-Height-Heading-H3, 28px); /* 116.667% */
      }  
      a{
        color: #000;
        text-decoration: underline;        
      }
      li {
        margin: 0 24px 16px;
        list-style: disc;
      }      
    }
    .review-wrapper{
      max-width: 840px;   
      display: flex;
      flex-direction: column;
      gap: var(--Spacing-Container-sm, 16px);
     
      @media(max-width: 768px){
        max-width: 313px;          
      }
    }
     .review-wrapper p {
        font-size: var(--Font-Size-Heading-H2, 24px);
        line-height: 32px;
          margin-bottom: 0;
      }
    .review-bottom{
      display: flex;
      align-items: center;    
      {% if section.settings.content_alignment == 'left' %}
        justify-content: start;
      {% elsif section.settings.content_alignment == 'right' %}
        justify-content: end;
      {% else %}
        justify-content: center;
      {% endif %}
    }
    .name{
      color: var(--Content-Base-main, #1F2937);
      text-align: center;
      margin: 0;
      /* font-family: var(--font-family-Body, "Neue Plak"); */
      font-size: 20px;
      font-style: normal;
      font-weight: 400;
      line-height: 24px; /* 120% */
      letter-spacing: 1.2px;
      text-transform: uppercase;      
    }
    .name span{
      color: var(--Content-Base-main, #1F2937);
      margin: 0;
      /* font-family: var(--font-family-Body, "Neue Plak"); */
      font-size: var(--Font-Size-Paragraph-Regular, 16px);
      font-style: normal;
      font-weight: 400;
      line-height: var(--Line-Height-Paragraph-Regular, 24px);
      letter-spacing: normal;
      text-transform: capitalize;
    }
    .buttons-wrapper{
      width: 100%;
      display: flex;
      {% if section.settings.content_alignment == 'left' %}
        justify-content: start;
      {% elsif section.settings.content_alignment == 'right' %}
        justify-content: end;
      {% else %}
        justify-content: center;
      {% endif %}
    }
    .buttons-wrapper a{
      font-family: var(--font-family-Control, "Neue Plak");
    }
    .dark{
      cursor: pointer;
      color: #FFF;
      text-align: center;
      /* font-family: var(--font-family-Body, "Neue Plak"); */
      font-size: var(--Font-Size-UI-Controls-Control-Button-Small, 20px);
      line-height: var(--Line-Height-UI-Controls-Control-Button-Small, 20px);
      text-transform: uppercase;
      /* border-radius: var(--rounded-3xl, 24px); */
      background: #212B36;
      display: inline-block;
      padding: var(--Spacing-Container-xs) var(--Spacing-Container-lg);
      max-width: max-content;     
    }
    .light {
      cursor: pointer;
      display: inline-block;
      /* border-radius: var(--rounded-3xl); */
      border: 2px solid var(--Content-Base-main, #1f2937);
      background: #eeedf7;
      padding: var(--Spacing-Container-xs) var(--Spacing-Container-lg);
      color: var(--Content-Base-main);
      text-align: center;
      /* font-family: var(--font-family-Body, "Neue Plak"); */
      font-size: var(--Font-Size-UI-Controls-Control-Button-Regular);
      font-style: normal;
      font-weight: 400;
      line-height: var(--Line-Height-UI-Controls-Control-Button-Regular);
      text-transform: uppercase;
      transition: all 0.4s;
    }
    .light:before,
    .light:after {
      content: unset !important;
    }
    .light:hover {
      background: #1f2937;
      color: #eeedf7;
    }
    .richtext-wrapper.small {
      li, p, a, span {
        font-family: var(--font-family-body);
        font-size: 12px;        
      }
    }  
    .text-wrapper.small p{
        font-family: var(--font-family-body);
        font-size: 12px;        
    }     

    .richtext-wrapper.large {
      li, p, a, span {
        font-family: var(--font-family-body);
        font-size: 20px;        
      }
    }  
    .text-wrapper.large p{
        font-family: var(--font-family-body);
        font-size: 20px;        
    }      
  }
</style>

<div class="sec-{{ id }}">
  <div class="{% if section.settings.width == 'page-width' %}page-width{% elsif section.settings.width == "page-width-narrow" %}page-width page-width--narrow{% else %} {% endif %}">
    <div class="wrapper-{{ id }}">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'text' -%}
            <div class="text-wrapper {{ block.settings.font_size }}">
              <p class="text">
                {{ block.settings.text }}
              </p>
            </div>
          {%- when 'title' -%}
            <h2 class="title
              ">
              {{ block.settings.title }}
            </h2>
          {%- when 'liquid' -%}
            <div class="liquid--{{ id }}">
              {{ block.settings.liquid }}
            </div>              
          {%- when 'richtext' -%}
            <div class="richtext-wrapper {{ block.settings.font_size }}">
              {{ block.settings.richtext }}
            </div>
          {%- when 'button' -%}
            <div class="buttons-wrapper">
              {% if block.settings.button_label != blank %}
                <a href="{{ block.settings.button_link }}" class="{% if block.settings.btn_color == 'light' %}button{% else %}button{% endif %} button">{{ block.settings.button_label }}</a>
              {% endif %}
            </div>
          {%- when 'review' -%}
            <div class="review-wrapper">
              <div class="richtext-wrapper">
                {{ block.settings.review_text }}
              </div>
              <div class="review-bottom">
                {% if block.settings.name != blank %}
                  <p class="name">{{ block.settings.name }} <span>{{ block.settings.designation }}</span></p>
                {% endif %}
              </div>
            </div>
        {% endcase %}
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Featured Text",
  "settings": [
    {
      "type": "select",
      "id": "width",
      "options": [
        {
          "value": "full",
          "label": "Full"
        },
        {
          "value": "page-width",
          "label": "Page Wiidth"
        },
        {
          "value": "page-width-narrow",
          "label": "Page Wiidth Narrow"
        }
      ],
      "default": "page-width",
      "label": "Section Width"
    },
    {
      "type": "number",
      "id": "max_width",
      "label": "Max Width"
    },      
    {
      "type": "select",
      "id": "content_alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center",
      "label": "Content Alignment"
    },
    {
      "type": "header",
      "content": "Section Spacing Desktop"
    },
    {
      "type": "range",
      "id": "margin_top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Margin Top",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Margin Bottom",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding Top",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 48
    },
    {
      "type": "header",
      "content": "Section Spacing Mobile"
    },
    {
      "type": "range",
      "id": "margin_top_m",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Margin Top",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_bottom_m",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Margin Bottom",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_m",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding Top",
      "default": 32
    },
    {
      "type": "range",
      "id": "padding_bottom_m",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 32
    }
  ],
  "blocks": [
    {
      "type": "title",
      "name": "Title",
      "limit": 1,
      "settings": [
        {
          "type": "inline_richtext",
          "id": "title",
          "label": "Title"
        }
      ]
    },
    {
      "type": "liquid",
      "name": "Liquid",
      "limit": 1,
      "settings": [
        {
          "type": "liquid",
          "id": "liquid",
          "label": "Liquid"
        }
      ]
    },    
    {
      "type": "text",
      "name": "Text",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text"
        },
        {
          "type": "select",
          "id": "font_size",
          "label": "Font Size",
          "options": [
            {
              "value": "large",
              "label": "Large"
            },            
            {
              "value": "medium",
              "label": "Medium"
            },
            {
              "value": "small",
              "label": "Small"
            },
          ],
          "default": "medium"
        },           
      ]
    },
    {
      "type": "richtext",
      "name": "Richtext",
      "limit": 1,
      "settings": [
        {
          "type": "richtext",
          "id": "richtext",
          "label": "Richtext"
        },
        {
          "type": "select",
          "id": "font_size",
          "label": "Font Size",
          "options": [
            {
              "value": "large",
              "label": "Large"
            },            
            {
              "value": "medium",
              "label": "Medium"
            },
            {
              "value": "small",
              "label": "Small"
            },
          ],
          "default": "medium"
        },           
      ]
    },
    {
      "type": "button",
      "name": "Button",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Url"
        },
        {
          "type": "select",
          "id": "btn_color",
          "label": "Button Color scheme",
          "options": [
            {
              "value": "dark",
              "label": "Dark Mode"
            },
            {
              "value": "light",
              "label": "Light Mode"
            },
          ],
          "default": "light"
        },
      ]
    },
    {
      "type": "review",
      "name": "Review",
      "limit": 1,
      "settings": [
        {
          "type": "richtext",
          "id": "review_text",
          "label": "Review"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name"
        },
        {
          "type": "text",
          "id": "designation",
          "label": "Designation"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Featured Text"
    }
  ]
}
{% endschema %}
